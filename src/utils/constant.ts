const ARGS_NAME = {
  crashReporterDirectory: 'crash-reporter-directory',
  enableCrashReporter: 'enable-crash-reporter',
  disableCrashReporter: 'disable-crash-reporter',
  crashReporterId: 'crash-reporter-id'
}

const GLOBAL_NAMES = {
  getOpenUrls: 'getOpenUrls',
  macOpenFiles: 'macOpenFiles'
}

const PROCESS_ENV = {
  MY_VSCODE_DEV: 'MY_VSCODE_DEV',
  MY_VSCODE_CWD: 'MY_VSCODE_CWD'
}

const MIN_MAX_MEMORY_SIZE_MB = 2048;

export {
  ARGS_NAME,
  PROCESS_ENV,
  GLOBAL_NAMES,
  MIN_MAX_MEMORY_SIZE_MB
}
